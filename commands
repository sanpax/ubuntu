#shutdown server
shutdown -P now

#scan for ports on specific ip
nmap -sV -p 1-65535 192.168.1.1/24

#ifconfig und Co installieren
apt-get install net-tools

#read out dns information
# path /etc/resolv.conf
resolvectl status


